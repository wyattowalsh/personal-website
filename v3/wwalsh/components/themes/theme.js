import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#5f8fa2',
    },
    secondary: {
      main: '#a2725f',
    },
  },
  typography: {
    fontFamily: 'Ubuntu',
  },
});