Repo conventions: prefer server components by default; use 'use client' only when browser interactivity is needed. Use existing shadcn/ui primitives in components/ui/, use @/ path aliases for imports, and use cn() from lib/utils for className composition. Favor named exports, PascalCase component filenames, use* hook names, camelCase utilities, and surgical edits over rewrites. Preserve MDX frontmatter structure in content/posts/{slug}/index.mdx and reference public assets as /filename.